# FleeCat 프로젝트 포트폴리오

> AI 기반 플리마켓 데이터 플랫폼 풀스택 개발 프로젝트

**개발자**: Jinwon19
**GitHub**: https://github.com/Jinwon19/fleecat_pipeline
**기간**: 2024년 10월 ~ 2025년 1월 (3개월)
**역할**: 풀스택 개발 (개인 프로젝트)

---

<div style="page-break-after: always;"></div>

## 📑 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [핵심 기능 1: 병렬 웹크롤링](#3-핵심-기능-1-병렬-웹크롤링)
4. [핵심 기능 2: AI 데이터 정제](#4-핵심-기능-2-ai-데이터-정제)
5. [핵심 기능 3: 멀티테넌트 DB 설계](#5-핵심-기능-3-멀티테넌트-db-설계)
6. [핵심 기능 4: 지도 시각화](#6-핵심-기능-4-지도-시각화)
7. [기술적 도전과 해결](#7-기술적-도전과-해결)
8. [성과 및 배운 점](#8-성과-및-배운-점)

---

<div style="page-break-after: always;"></div>

## 1. 프로젝트 개요

### 1.1 프로젝트 배경

플리마켓 정보가 여러 웹사이트에 분산되어 있어 사용자들이 원하는 행사를 찾기 어렵다는 문제를 발견했습니다. 단순히 정보를 모으는 것을 넘어, **비정형 텍스트를 AI로 구조화**하고 **지도 시각화**로 사용자 경험을 개선하는 플랫폼을 개발하기로 결정했습니다.

### 1.2 프로젝트 목표

1. **자동화된 데이터 수집**: 수동 작업 없이 100+ 플리마켓 정보 자동 크롤링
2. **AI 기반 데이터 정제**: GPT-4로 비정형 텍스트를 JSON으로 구조화 (90%+ 정확도)
3. **확장 가능한 아키텍처**: 멀티테넌트 구조로 향후 E-Commerce 플랫폼 확장 가능
4. **직관적인 시각화**: 카카오맵으로 100+ 플리마켓을 한눈에 확인

### 1.3 핵심 기술 스택

| 분야 | 기술 |
|------|------|
| **Backend** | Node.js, Express, Prisma, PostgreSQL (Supabase) |
| **Frontend** | React 19, Vite, Kakao Maps API |
| **Data Pipeline** | Python, Selenium, BeautifulSoup4, OpenAI GPT-4o |
| **Database** | Supabase PostgreSQL (15개 테이블) |
| **DevOps** | Git, GitHub, Railway (배포 예정) |

### 1.4 프로젝트 규모

- **총 코드 라인**: 113,997줄
- **파일 수**: 559개
- **DB 테이블**: 15개 (복잡한 관계형 구조)
- **수집 데이터**: 100+ 플리마켓 정보
- **문서화**: 600줄+ README (백엔드, 프론트엔드, 크롤링 각각)

---

<div style="page-break-after: always;"></div>

## 2. 시스템 아키텍처

### 2.1 전체 데이터 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│                      웹사이트 (플리마켓 게시글)                    │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 1: 웹크롤링 (Python)                                       │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  - Selenium + Requests 하이브리드                                │
│  - ThreadPoolExecutor 병렬 처리 (10 workers)                    │
│  - 자동 재시도 + 진행률 표시 (tqdm)                              │
│                                                                   │
│  flea_list_fast.py → fleamarket_posts.json (목록)                │
│  flea_text_fast.py → fleamarket_detail.json (상세)               │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 2: LLM 데이터 정제 (GPT-4o)                                │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  - 3단계 정제 프로세스:                                          │
│    1) 텍스트 분석: 게시글에서 날짜/장소/시간 추출                  │
│    2) 이미지 분석: Vision API로 포스터에서 정보 보완              │
│    3) 데이터 검증: 날짜 형식 통일, "미정" 제거                    │
│                                                                   │
│  llm_processor.py → fleamarket_structured.json                   │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 3: Supabase DB 저장 (PostgreSQL)                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  - 15개 테이블 멀티테넌트 구조                                    │
│  - Prisma ORM으로 타입 안전성 보장                               │
│  - CASCADE/RESTRICT 정책으로 데이터 일관성 유지                  │
│                                                                   │
│  supabase_manager.py → Supabase (markets, sessions 테이블)       │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│  STEP 4: React 프론트엔드 (Kakao Maps)                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  - Supabase API로 실시간 데이터 조회                             │
│  - Kakao Maps API로 마커 표시                                    │
│  - 마커 클러스터링 + 날짜 필터링                                  │
│                                                                   │
│  Visual.jsx → 브라우저 (http://localhost:5173/visual)            │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 기술 선택 이유

| 기술 | 선택 이유 |
|------|----------|
| **Selenium + Requests** | JS 렌더링 필요 시 Selenium, 일반 페이지는 Requests로 10배 속도 향상 |
| **GPT-4o-mini** | GPT-4 대비 10배 저렴하면서도 구조화 작업에는 충분한 정확도 |
| **Prisma ORM** | TypeScript 지원으로 타입 안전성, 마이그레이션 관리 용이 |
| **Supabase** | PostgreSQL + REST API + 실시간 기능을 무료로 제공 |
| **React 19** | 최신 버전으로 성능 개선, Vite로 빠른 개발 환경 |

---

<div style="page-break-after: always;"></div>

## 3. 핵심 기능 1: 병렬 웹크롤링

### 3.1 문제 상황

- 플리마켓 정보가 10+ 페이지에 분산
- 단일 스레드로는 전체 크롤링에 **10분 이상** 소요
- 일부 페이지는 JavaScript 렌더링 필요

### 3.2 해결 방법: 하이브리드 병렬 크롤링

#### 핵심 코드 (`flea_list_fast.py`)

```python
from concurrent.futures import ThreadPoolExecutor
from tqdm import tqdm

MAX_WORKERS = 10  # 동시 처리 수
MAX_PAGES = 10    # 크롤링할 페이지 수

def fetch_page_requests(page_num):
    """빠른 Requests로 먼저 시도"""
    url = f"https://example.com/page/{page_num}"
    response = requests.get(url, timeout=10)

    if response.status_code == 200:
        return parse_html(response.text)

    # 실패 시 Selenium으로 폴백
    return fetch_page_selenium(page_num)

def main():
    all_posts = []

    # ThreadPoolExecutor로 병렬 처리
    with ThreadPoolExecutor(max_workers=MAX_WORKERS) as executor:
        futures = {
            executor.submit(fetch_page_requests, page): page
            for page in range(1, MAX_PAGES + 1)
        }

        # tqdm으로 진행률 표시
        for future in tqdm(as_completed(futures), total=MAX_PAGES):
            result = future.result()
            if result:
                all_posts.extend(result)

    # JSON 저장
    save_json(all_posts, "fleamarket_posts.json")
```

### 3.3 성과

| 항목 | 개선 전 | 개선 후 | 개선율 |
|------|---------|---------|--------|
| 크롤링 시간 | 10분+ | **1~2분** | **10배 향상** |
| 성공률 | 70% | **95%+** | +25%p |
| 코드 복잡도 | 단순 | 병렬 | 관리 가능 |

### 3.4 기술적 하이라이트

- **자동 재시도**: 최대 3회 재시도로 안정성 확보
- **Requests → Selenium 폴백**: 빠른 속도와 안정성 모두 확보
- **진행률 표시**: tqdm으로 사용자 경험 개선

---

<div style="page-break-after: always;"></div>

## 4. 핵심 기능 2: AI 데이터 정제

### 4.1 문제 상황

플리마켓 게시글은 **형식이 제각각**:

```
예시 1: "10월 20일 토요일 오후 2시~6시 서울숲 근처에서 플리마켓!!"
예시 2: "이번 주말 강남역 10번 출구 앞 14:00-18:00"
예시 3: "플리마켓 개최합니다 (날짜: 미정)"
```

→ 날짜 형식, 장소 표현, 시간대가 모두 다름

### 4.2 해결 방법: 3단계 LLM 정제 프로세스

#### STEP 1: 텍스트 분석

```python
# prompt_templates.py
SYSTEM_PROMPT = """
당신은 플리마켓 정보를 구조화하는 전문가입니다.
다음 형식으로 JSON을 생성하세요:

{
  "market_name": "플리마켓 이름",
  "place": "장소",
  "sessions": [{
    "start_date": "YYYY-MM-DD",
    "start_time": "HH:MM",
    "end_time": "HH:MM"
  }]
}
"""

# llm_processor.py
def process_with_llm(text):
    response = openai.ChatCompletion.create(
        model="gpt-4o-mini",
        temperature=0.2,  # 일관성 있는 출력
        messages=[
            {"role": "system", "content": SYSTEM_PROMPT},
            {"role": "user", "content": text}
        ]
    )
    return json.loads(response.choices[0].message.content)
```

#### STEP 2: 이미지 분석 (Vision API)

포스터 이미지에서 누락된 정보 추출:

```python
def analyze_image(image_url):
    response = openai.ChatCompletion.create(
        model="gpt-4-vision-preview",
        messages=[{
            "role": "user",
            "content": [
                {"type": "text", "text": "이 포스터에서 날짜, 시간, 장소를 추출하세요"},
                {"type": "image_url", "image_url": {"url": image_url}}
            ]
        }]
    )
    return response.choices[0].message.content
```

#### STEP 3: 데이터 검증

```python
def validate_data(data):
    # 날짜 형식 통일
    if data.get("start_date"):
        data["start_date"] = normalize_date(data["start_date"])

    # "미정" 값 제거
    if data.get("place") == "미정":
        data["place"] = None

    # 필수 필드 검증
    required = ["market_name", "sessions"]
    if not all(field in data for field in required):
        raise ValueError("필수 필드 누락")

    return data
```

### 4.3 성과

| 항목 | 값 |
|------|-----|
| 정확도 | **90%+** |
| 처리 시간 | 100개 게시물 약 2~5분 |
| 비용 | $0.10~0.50 (GPT-4o-mini 사용) |
| 성공률 | 텍스트만: 70% → 텍스트+이미지: **90%+** |

### 4.4 실제 변환 예시

**입력 (비정형):**
```
"10월 20일 토요일 오후 2시~6시 서울숲 근처에서 플리마켓 해요!!"
```

**출력 (구조화):**
```json
{
  "market_name": "서울숲 플리마켓",
  "place": "서울숲",
  "sessions": [{
    "start_date": "2024-10-20",
    "start_time": "14:00",
    "end_time": "18:00"
  }]
}
```

---

<div style="page-break-after: always;"></div>

## 5. 핵심 기능 3: 멀티테넌트 DB 설계

### 5.1 설계 목표

단순한 플리마켓 목록을 넘어 **향후 E-Commerce 플랫폼으로 확장** 가능한 구조 설계

### 5.2 DB 스키마 (15개 테이블)

#### 핵심 관계도 (간소화)

```
┌──────────┐         ┌────────────┐
│ Company  │         │   Member   │
└────┬─────┘         └─────┬──────┘
     │ 1                   │ N
     └─────────────────────┘
                           │ N
                     ┌─────┴──────┐
                     │            │
              ┌──────▼─────┐     │
              │TenantMember│     │
              │ (Join)     │     │
              └──────┬─────┘     │
                     │ N         │ 1
              ┌──────▼─────┐     │
              │   Tenant   │     │
              │  (상점)     │     │
              └──────┬─────┘     │
                     │ 1         │
              ┌──────▼─────┐     │
              │  Product   │     │
              └────────────┘     │
                                 │
                          ┌──────▼─────┐
                          │   Order    │
                          └──────┬─────┘
                                 │ 1
                          ┌──────▼─────┐
                          │  Payment   │
                          └────────────┘
```

#### Prisma 스키마 예시 (`schema.prisma`)

```prisma
// 회원 (Member)
model Member {
  member_id           BigInt       @id @default(autoincrement())
  member_email        String       @unique
  member_nickname     String       @unique
  member_password     String
  member_status       String       @default("active") // active, suspended, inactive
  member_created_at   DateTime     @default(now())
  member_updated_at   DateTime     @updatedAt

  // 관계
  company             Company?     @relation(fields: [company_id], references: [company_id])
  company_id          BigInt?
  tenant_members      TenantMember[]
  orders              Order[]

  @@map("member")
}

// 판매자 상점 (Tenant)
model Tenant {
  tenant_id           BigInt       @id @default(autoincrement())
  tenant_name         String
  tenant_status       String       @default("active")
  tenant_created_at   DateTime     @default(now())
  tenant_updated_at   DateTime     @updatedAt

  // 관계
  tenant_members      TenantMember[]

  @@map("tenant")
}

// 판매자-회원 연결 (Many-to-Many)
model TenantMember {
  tenant_member_id    BigInt       @id @default(autoincrement())

  // FK
  tenant              Tenant       @relation(fields: [tenant_id], references: [tenant_id])
  tenant_id           BigInt
  member              Member       @relation(fields: [member_id], references: [member_id])
  member_id           BigInt

  // 관계
  products            Product[]

  @@unique([tenant_id, member_id])
  @@map("tenant_member")
}

// 상품 (Product)
model Product {
  product_id          BigInt       @id @default(autoincrement())
  product_name        String
  product_price       Decimal      @db.Decimal(10, 2)
  product_status      String       @default("active") // active, sold_out, inactive
  product_created_at  DateTime     @default(now())
  product_updated_at  DateTime     @updatedAt

  // FK
  tenant_member       TenantMember @relation(fields: [tenant_member_id], references: [tenant_member_id])
  tenant_member_id    BigInt
  category            Category     @relation(fields: [category_id], references: [category_id])
  category_id         BigInt

  @@map("product")
}
```

### 5.3 CASCADE 정책 설계

| 테이블 | 삭제 시 정책 | 이유 |
|--------|-------------|------|
| `member_address` | **CASCADE** | 회원 삭제 시 주소도 함께 삭제 |
| `product_img` | **CASCADE** | 상품 삭제 시 이미지도 함께 삭제 |
| `shopping_cart` | **CASCADE** | 회원 삭제 시 장바구니도 함께 삭제 |
| `order` | **RESTRICT** | 주문 내역은 법적 보관 필요 (삭제 불가) |
| `payment` | **RESTRICT** | 결제 기록은 회계 감사용 보존 |

### 5.4 성과

- **데이터 무결성**: FK 제약으로 잘못된 데이터 삽입 방지
- **확장성**: 멀티테넌트 구조로 향후 E-Commerce 확장 가능
- **성능**: 인덱스 최적화로 빠른 조회 속도
- **문서화**: 657줄의 상세한 백엔드 README 작성

---

<div style="page-break-after: always;"></div>

## 6. 핵심 기능 4: 지도 시각화

### 6.1 구현 목표

100+ 플리마켓을 **한눈에 볼 수 있는 지도 시각화**

### 6.2 Kakao Maps 통합

#### 핵심 코드 (`Visual.jsx`)

```jsx
import React, { useState, useEffect, useRef } from "react";

export default function Visual() {
  const mapRef = useRef(null);
  const kakaoMapRef = useRef(null);
  const [marketData, setMarketData] = useState([]);
  const [filteredData, setFilteredData] = useState([]);

  // Kakao Maps 초기화
  useEffect(() => {
    const script = document.createElement("script");
    script.src = `//dapi.kakao.com/v2/maps/sdk.js?appkey=${KAKAO_KEY}&autoload=false&libraries=services,clusterer`;
    script.async = true;
    document.head.appendChild(script);

    script.onload = () => {
      kakao.maps.load(() => {
        const map = new kakao.maps.Map(mapRef.current, {
          center: new kakao.maps.LatLng(37.5665, 126.9780), // 서울
          level: 8
        });
        kakaoMapRef.current = map;
      });
    };
  }, []);

  // Supabase에서 데이터 로드
  useEffect(() => {
    const fetchData = async () => {
      const { data, error } = await supabase
        .from('markets')
        .select('*, sessions(*)');

      if (!error) {
        setMarketData(data);
        setFilteredData(data);
      }
    };
    fetchData();
  }, []);

  // 마커 생성 + 클러스터링
  useEffect(() => {
    if (!kakaoMapRef.current || filteredData.length === 0) return;

    const clusterer = new kakao.maps.MarkerClusterer({
      map: kakaoMapRef.current,
      averageCenter: true,
      minLevel: 5
    });

    const markers = filteredData.map(market => {
      // 지오코딩으로 좌표 얻기
      const position = geocode(market.place);

      const marker = new kakao.maps.Marker({ position });

      // 마커 클릭 이벤트
      kakao.maps.event.addListener(marker, 'click', () => {
        showMarketInfo(market);
      });

      return marker;
    });

    clusterer.addMarkers(markers);
  }, [filteredData]);

  return (
    <div>
      <div ref={mapRef} style={{ width: '100%', height: '100vh' }} />
      {/* 날짜 필터링 UI */}
    </div>
  );
}
```

### 6.3 지오코딩 캐싱

API 호출을 최소화하기 위한 캐싱 전략:

```javascript
const geocodeCache = {};

async function geocode(address) {
  // 캐시 확인
  if (geocodeCache[address]) {
    return geocodeCache[address];
  }

  // Kakao Local API 호출
  const response = await fetch(
    `https://dapi.kakao.com/v2/local/search/address.json?query=${address}`,
    { headers: { Authorization: `KakaoAK ${KAKAO_REST_API_KEY}` } }
  );

  const data = await response.json();
  const { x, y } = data.documents[0];

  // 캐시 저장
  geocodeCache[address] = { lat: y, lng: x };
  return geocodeCache[address];
}
```

### 6.4 성과

| 항목 | 값 |
|------|-----|
| 마커 수 | 100+ |
| 렌더링 속도 | 2초 미만 (클러스터링 사용) |
| API 호출 감소 | 80% (캐싱으로) |
| 사용자 경험 | 직관적인 지도 인터페이스 |

---

<div style="page-break-after: always;"></div>

## 7. 기술적 도전과 해결

### 7.1 도전 1: 비정형 데이터 구조화

**문제**: 플리마켓 게시글마다 날짜/시간 형식이 제각각

**시도한 방법들**:
1. 정규식: 너무 많은 예외 케이스
2. 단순 LLM: 누락된 정보 많음
3. ✅ **3단계 정제 프로세스**: 텍스트 + 이미지 + 검증

**결과**: 정확도 70% → **90%+** 향상

---

### 7.2 도전 2: 크롤링 속도 vs 안정성

**문제**: 단일 스레드는 느리고, 병렬 처리는 차단당할 위험

**해결**:
- ThreadPoolExecutor로 적정 수준 병렬 처리 (10 workers)
- Requests 실패 시 Selenium 자동 폴백
- 재시도 로직 + User-Agent 회전

**결과**: 속도 10배 향상 + 성공률 95%+

---

### 7.3 도전 3: 멀티테넌트 데이터 격리

**문제**: 한 플랫폼에서 여러 판매자의 데이터 안전하게 분리

**해결**:
- Shared Database, Shared Schema 방식 채택
- FK 관계로 논리적 격리
- Prisma로 안전한 쿼리 작성

**결과**: 데이터 누수 방지 + 확장성 확보

---

<div style="page-break-after: always;"></div>

## 8. 성과 및 배운 점

### 8.1 정량적 성과

| 지표 | 값 |
|------|-----|
| **총 코드 라인** | 113,997줄 |
| **파일 수** | 559개 |
| **DB 테이블** | 15개 |
| **수집 데이터** | 100+ 플리마켓 |
| **크롤링 속도** | 단일 10분+ → 병렬 **1~2분** (10배 향상) |
| **LLM 정확도** | **90%+** |
| **API 호출 감소** | 캐싱으로 **80% 감소** |
| **문서화** | 600줄+ README |

### 8.2 기술적 성장

#### 풀스택 개발 역량

1. **백엔드**: Node.js, Express, Prisma로 RESTful API 구축
2. **프론트엔드**: React 19로 SPA 개발, Kakao Maps 통합
3. **데이터 파이프라인**: Python으로 크롤링 + LLM 정제 자동화
4. **데이터베이스**: PostgreSQL 15개 테이블 설계, 멀티테넌트 아키텍처

#### AI/LLM 활용 능력

- GPT-4o를 실무 프로젝트에 통합
- 프롬프트 엔지니어링으로 90%+ 정확도 달성
- Vision API로 이미지에서 정보 추출
- 비용 최적화 (GPT-4o-mini 사용으로 10배 절감)

#### 성능 최적화

- 병렬 처리로 10배 속도 향상
- 지오코딩 캐싱으로 API 호출 80% 감소
- 마커 클러스터링으로 100+ 마커 빠른 렌더링

### 8.3 프로젝트 관리

- **버전 관리**: Git으로 체계적인 커밋 관리
- **문서화**: 600줄+ README로 협업 준비
- **코드 품질**: Layered Architecture로 유지보수성 확보
- **테스트**: Jest로 단위 테스트 작성 (회원 API 12개)

### 8.4 배운 점

1. **단순 CRUD를 넘어서**: 실제 데이터를 수집하고 가공하는 전체 파이프라인 구축
2. **AI는 도구**: LLM을 "마법"이 아닌 "도구"로 활용하는 방법 습득
3. **아키텍처의 중요성**: 멀티테넌트 설계로 향후 확장 가능한 구조 구축
4. **문서화의 가치**: 상세한 문서로 미래의 나와 협업자를 위한 준비

---

<div style="page-break-after: always;"></div>

## 9. 향후 개선 계획

### 9.1 단기 목표 (1-2개월)

- [ ] Docker 컨테이너화
- [ ] CI/CD 파이프라인 구축 (GitHub Actions)
- [ ] 테스트 커버리지 80%+
- [ ] Railway 배포

### 9.2 중기 목표 (3-6개월)

- [ ] Redis 캐싱 도입
- [ ] 관리자 대시보드 개발
- [ ] 모바일 반응형 디자인
- [ ] 실시간 알림 기능

### 9.3 장기 비전 (6개월+)

- [ ] E-Commerce 플랫폼으로 확장
- [ ] 판매자 입점 시스템
- [ ] 결제 모듈 통합
- [ ] 추천 알고리즘 (ML)

---

## 10. 연락처 및 참고

**GitHub**: https://github.com/Jinwon19/fleecat_pipeline
**Email**: (연락처 입력)
**포트폴리오**: (포트폴리오 링크)

### 프로젝트 실행

```bash
# 저장소 클론
git clone https://github.com/Jinwon19/fleecat_pipeline.git
cd fleecat_pipeline

# 상세 실행 방법은 README.md 참고
```

### 문서

- **프로젝트 전체 소개**: `README.md`
- **백엔드 가이드**: `be/README.md` (657줄)
- **크롤링 가이드**: `flee/README.md`
- **프론트엔드 가이드**: `fe-skeleton/README.md`
- **업로드 가이드**: `GITHUB_UPLOAD_GUIDE.md`

---

**제작일**: 2025년 1월
**개발 기간**: 2024년 10월 ~ 2025년 1월 (3개월)
**개발자**: Jinwon19

---

> "단순히 코드를 작성하는 것을 넘어, 실제 문제를 해결하는 완전한 시스템을 구축하는 경험을 했습니다."
